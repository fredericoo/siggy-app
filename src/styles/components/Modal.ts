const Modal = {
  parts: ['dialog'],
  variants: {
    custom: {
      dialog: {
        borderRadius: 'xl',
      },
    },
  },
  defaultProps: {
    variant: 'custom',
  },
};

export default Modal;
